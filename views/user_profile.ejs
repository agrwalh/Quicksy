<%- include('./partials/header', { cartCount: cartCount, categories: categories, user: user }) %>
<div class="flex flex-col items-center justify-center min-h-[60vh]">
    <div class="bg-white p-8 rounded shadow max-w-md w-full">
        <h2 class="text-2xl font-bold mb-6 text-green-700 text-center">Profile</h2>
        <div class="mb-4">
            <span class="font-semibold">Name:</span> <%= user.name || 'N/A' %>
        </div>
        <div class="mb-4">
            <span class="font-semibold">Email:</span> <%= user.email || 'N/A' %>
        </div>
        <a href="/" class="block text-center text-green-700 mt-4 hover:underline">Back to Home</a>
    </div>
</div>
<%- include('./partials/footer') %> 