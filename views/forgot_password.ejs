<%- include('./partials/header', { cartCount: 0, categories: categories || [], user: null }) %>
<div class="flex flex-col items-center justify-center min-h-[60vh] bg-gradient-to-br from-green-50 via-white to-pink-50">
  <div class="bg-white p-8 rounded-2xl shadow-xl max-w-md w-full text-center border-t-8 border-green-500">
    <h2 class="text-2xl font-bold mb-4 text-green-700">Forgot Password?</h2>
    <p class="mb-4 text-gray-600">Enter your email address to receive a 4-digit OTP for password reset.</p>
    <form action="/users/forgot-password" method="POST" class="space-y-4 text-left">
      <div>
        <label class="block text-gray-700 font-semibold mb-1">Email</label>
        <input type="email" name="email" class="w-full px-4 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-green-400" placeholder="Enter your email" required>
      </div>
      <button type="submit" class="w-full bg-green-600 text-white font-bold py-2 rounded-xl shadow-lg hover:bg-green-700 transition text-lg mt-2">Send OTP</button>
    </form>
  </div>
</div>
<%- include('./partials/footer') %> 